# Dependabot — auto PRs for dependency updates.
# Spec: docs/DEVELOPMENT_PLAN.md §0 (engineering hygiene)
#
# We group most updates by ecosystem so we don't drown in tiny PRs. Major
# updates remain separate so they get explicit review.

version: 2
updates:
  - package-ecosystem: npm
    directory: '/'
    schedule:
      interval: weekly
      day: monday
    open-pull-requests-limit: 5
    ignore:
      # `@types/vscode` tracks `engines.vscode`, not the latest release, and is
      # pinned exactly (no caret) so the compiler enforces the floor instead of
      # merely declaring it. `engines.vscode` is the OLDEST editor the extension
      # supports; types newer than that let a call added in a later VS Code
      # typecheck cleanly and then fail on the editor we promised to run on.
      # `vsce package` refuses the mismatch outright:
      #   ERROR  @types/vscode ^1.125.0 greater than engines.vscode ^1.85.0
      # Taking such a bump means dropping every VS Code in between — a support
      # decision, not a dependency update. Raise `engines.vscode` deliberately
      # and move the types with it, together.
      - dependency-name: '@types/vscode'
      # Same rule, same reason: `@types/node` describes the OLDEST Node we
      # support (`engines.node`, which CI pins), not the newest release. Types
      # above that floor let a call added in a later Node compile and then throw
      # on the version we ship for — and unlike `@types/vscode` nothing refuses
      # the build, so it fails in a user's terminal instead. Minor and patch
      # inside the supported major still flow through the `typescript` group.
      - dependency-name: '@types/node'
        update-types: ['version-update:semver-major']
    groups:
      typescript:
        patterns:
          - 'typescript'
          - '@types/*'
          - 'typescript-eslint'
        update-types:
          - 'minor'
          - 'patch'
      eslint:
        patterns:
          - 'eslint'
          - '@eslint/*'
        update-types:
          - 'minor'
          - 'patch'
      testing:
        patterns:
          - 'vitest'
          - '@vitest/*'
        update-types:
          - 'minor'
          - 'patch'
      react:
        patterns:
          - 'react'
          - 'react-dom'
          - '@types/react'
          - '@types/react-dom'
        update-types:
          - 'minor'
          - 'patch'

  - package-ecosystem: github-actions
    directory: '/'
    schedule:
      interval: weekly
