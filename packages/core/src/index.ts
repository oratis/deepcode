// @deepcode/core — kernel for DeepCode
// See docs/DEVELOPMENT_PLAN.md §3 for module structure.
// M0 skeleton — modules are placeholders, real implementation per milestone.

export const VERSION = '0.0.0';
export const PROJECT_NAME = 'DeepCode';

// Re-export key types for convenience (filled out as modules ship)
export type * from './types.js';
